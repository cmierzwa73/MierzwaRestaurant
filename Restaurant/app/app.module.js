//uses an IIFE (immediately-invoked function expression) to immediately execute this code and also to encapsulate its scope
(function () {
    'use strict'; //is optional : triggers modern browsers to run the JavaScript in strict mode

    angular.module('app', []); //registers the module with the Angular framework

})();
